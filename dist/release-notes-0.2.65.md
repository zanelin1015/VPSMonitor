## v0.2.65

- 授权选择改为 Client / 节点 / 客户端三层结构，支持只授权某个节点下的部分客户端。
- 区域账号授权弹窗同步拆分 Realm 入口 Client 与 x-ui 出口 Client，并以三层结构保存/展示授权范围。
- 用户链路、区域账号可管理范围、下属用户链路列表同步改为三层层级展示，便于看清哪个 Client、哪个节点、哪些客户端被授权。
- x-ui 与 Realm 客户端列表改为按 Client 和节点分组展示；区域账号视图继续隐藏目标 Client、命中节点等内部细节。
