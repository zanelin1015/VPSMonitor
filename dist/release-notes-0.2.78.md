# VPSMonitor v0.2.78

## 变更
- 移除客户端“周期刷新 / 是否跟随收费周期”的独立开关。
- 客户端设置了开始时间后，系统始终按照收费周期（月/季/年）滚动计算当前周期到期时间。
- 收费周期变更时，客户端到期时间会直接按新的收费周期重新计算。
- 后端同步任务不再根据独立开关判断是否禁用客户端；有开始时间的客户端只按收费周期滚动同步 x-ui 到期时间。

## 验证
- `go test ./...`
- `npm run build`
- `./scripts/build.sh`
