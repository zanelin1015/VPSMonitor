## v0.2.54

- 拓扑匹配现在会把 Client「主域名 / import_domain」纳入目标入站候选，修复已设置 COX 域名但出站仍无法匹配到下游节点的问题。
- 用户链路 / Realm 入口判断目标 Client 时同样支持主域名，A/C 转发到 B 的域名入口可以正确识别目标机器。
- 新增回归测试覆盖：出站指向目标 Client 主域名 + 端口时，应匹配到对应 x-ui 入站并继续生成完整客户端链路。
