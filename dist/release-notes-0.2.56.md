## v0.2.56

- 端口限速支持采集 VPS 当前系统中的 tc 规则，并在端口策略中展示和作为可编辑初始值。
- 端口限速下发时按端口去重，同一个端口只保留一组限速规则；修改 Mbps 会重建 tc 并覆盖旧限速。
- 端口限速改为双向 tc：出站按源端口限速，入站按目标端口 police 限速，减少流量落到默认 class 的情况。
- x-ui 出站为空时自动补充隐式 direct/freedom 默认出站，避免新版 3x-ui 显示出站为空。
- 拓扑匹配支持使用 VPS 上采集到的非主证书域名命中链路。
