## v0.2.58

- Client 采集端口策略时新增 `ufw status numbered` 解析，系统中已有 UFW 白名单会显示到端口策略页。
- UFW 的 TCP/UDP 同端口白名单会自动合并为 `TCP + UDP`，多个来源 IP 会合并到同一端口规则。
- 当前系统端口策略现在会同时合并展示 UFW 白名单和 tc 限速，并可一键填入托管配置。
- 前端端口策略文案从仅 tc 限速调整为 UFW 白名单 + tc 限速的统一端口策略视图。
